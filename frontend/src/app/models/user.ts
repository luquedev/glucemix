export class User {
    constructor(
        id: number,
        password: string,
        email: string,
        username?: string,
        name?: string,
        lastname?: string,
        phone?: string,
        address?: string
    ) { }
}